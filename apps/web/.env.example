# Optional: override where the browser reaches the core engine. Leave unset
# for same-origin deployments (dev Vite proxy, Home Assistant addon) — the
# client resolves the API base from the document URL at runtime.
# PUBLIC_SERVER_URL=http://localhost:3000
