diff --git a/src/motion/tweened.js b/src/motion/tweened.js
index a24148d07594d82dd79097a8cf700959aa5d48aa..e9915e0acfe822f25d7a406080a2913e47458149 100644
--- a/src/motion/tweened.js
+++ b/src/motion/tweened.js
@@ -275,6 +275,10 @@ export class Tween {
 				}
 
 				previous_task?.abort();
+				// Drop the reference: the closure otherwise retains the previous task,
+				// whose closure retains the one before it, chaining every set() since
+				// the tween was created into an unbounded linked list (memory leak).
+				previous_task = null;
 			}
 
 			const elapsed = now - start;
